import { defineConfig } from 'vite'

// Simplified Vite config without @vitejs/plugin-react to avoid peer dependency issues.
export default defineConfig({
  // Keep default settings; React works with JSX via Vite's built-in support when using .jsx/.tsx files.
})
